#include "src/card.hxx"
#include <algorithm>
#include <cmath>
#include <cstddef>
#include <iomanip>
#include <iostream>
#include <iterator>
#include <magic_enum.hpp>
#include <map>
#include <random>
#include <string>
#include <sys/types.h>
#include <vector>

int
main ()
{
  // auto cardDeck = generateCardDeck ();
  // std::for_each (cardDeck.begin (), cardDeck.end (), [] (Card const &card) { std::cout << "card value: " << card.value << ", type: " << magic_enum::enum_name (card.type) << std::endl; });
  return 0;
}